# Leave blank when the frontend and backend share a Vercel deployment.
VITE_API_URL=http://127.0.0.1:8000
# Optional stable public address for confirmation and password-reset emails.
# When omitted, Bindit returns users to the same address where they signed up.
VITE_PUBLIC_APP_URL=http://127.0.0.1:5173
